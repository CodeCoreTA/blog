<%= simple_form_for [@post, @comment], remote: true do |f| %>
  <div class="form-group">
    <%= f.input :body, class: "form-control" %>
  </div>
  <div class="form-group">
    <%= f.submit value: "Submit", class: "btn btn-primary", data: {disable_with: "Saving..."} %>
  </div>
<% end %>
